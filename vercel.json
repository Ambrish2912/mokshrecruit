{
  "buildCommand": "cd my-app && npm ci && npm run build",
  "outputDirectory": "my-app/dist",
  "installCommand": "npm install --prefix ./my-app",
  "devCommand": "cd my-app && npm run dev",
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
